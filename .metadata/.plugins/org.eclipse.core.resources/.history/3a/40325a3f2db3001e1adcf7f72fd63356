package fuctional_interface;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class test {

	public static void main(String []args) {
		int s = FunctionalInterfac.add(1,3,4);
		//int a =s.add(5, 6,5);
		System.out.println();
		
		int [] nums = {1,3,546,78,9,79,9};
		
		// Convert the array to a list using Arrays.stream() and toList()
        List<Integer> numsList = Arrays.stream(nums).boxed().collect(Collectors.toList());

        // Find the maximum element using stream and max()
        int maxElement = numsList.stream().max(Integer::compare).get();

		System.out.println(maxElement);
		
	}
}
